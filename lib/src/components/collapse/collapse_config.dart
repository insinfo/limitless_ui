/// Default configuration for [LiCollapseDirective].
class LiCollapseConfig {
  LiCollapseConfig({
    this.animation = true,
    this.horizontal = false,
  });

  bool animation;
  bool horizontal;
}
