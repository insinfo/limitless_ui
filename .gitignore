.dart_tool/
.packages
/build/
/dist/
pubspec.lock
coverage/

referencias/
.local-chrome/

# Codecov CI uploader, downloaded into the package root. Keep it out of commits;
# .pubignore is what keeps it out of the published archive.
codecov
codecov.SHA256SUM
codecov.SHA256SUM.sig